{% extends 'base.html.twig' %}

{% block title %}Homepage - {{ parent() }}{% endblock %}


{% block body %}
    <div class="row">
        <div class="col-sm-12 text-center mt-5">
            <p class="mt-5 mb-5">Fell free to edit number in url after 'money/'</p>
        </div>
    </div>
    <div class="row mt-5">
        <div class="col-sm-6 text-center">
            <h1>{{ Usd }}</h1>
        </div>
        <div class="col-sm-6 text-center">
            <h1>{{ Eur }}</h1>
        </div>
    </div>
{% endblock %}
