{% extends 'base.html.twig' %}

{% block title %}Homepage - {{ parent() }}{% endblock %}


{% block body %}
    <div class="row">
        <div class="col-sm-12 text-center mt-5">
            <p class="mt-5 mb-5">Fell free to edit number in url after 'number/'</p>
            <h1 class="mt-5">{{ formatedNumber }}</h1>
        </div>
    </div>

{% endblock %}
